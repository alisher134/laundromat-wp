# Laundromat Static Site - Multi-Page

Многостраничный HTML сайт с Tailwind CSS для прачечной. Готов к переносу вёрстки из Next.js проекта.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd /Users/alisherrakhmanov134/Desktop/laundromat-static
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

### 3. Открытие сайта

**ВАЖНО:** Для работы общих компонентов (header/footer) нужен локальный сервер.

**Вариант 1 - VS Code Live Server (рекомендуется):**
1. Установите расширение "Live Server" в VS Code
2. Откройте любой HTML файл
3. Нажмите "Go Live" в правом нижнем углу

**Вариант 2 - Python сервер:**
```bash
python3 -m http.server 8000
```
Затем откройте http://localhost:8000

### 4. Сборка для продакшена

```bash
npm run build
```

---

## 📁 Структура проекта

```
laundromat-static/
├── index.html              # Главная страница
├── services.html           # Услуги
├── tips.html               # Советы
├── instructions.html       # Инструкции
├── faq.html                # FAQ
├── contact.html            # Контакты
├── location.html           # Локация
├── tip-detail.html         # Детали совета
├── 404.html                # Страница ошибки
│
├── includes/               # Общие компоненты
│   ├── header.html         # Шапка сайта (навигация)
│   └── footer.html         # Подвал сайта
│
├── assets/
│   ├── css/
│   │   └── output.css      # Скомпилированный Tailwind
│   └── js/
│       └── main.js         # JavaScript (загрузка компонентов, навигация)
│
├── src/
│   └── input.css           # Tailwind исходник
│
└── tailwind.config.js      # Конфигурация Tailwind
```

---

## 📄 Все 9 страниц

| Страница | Файл | Описание |
|----------|------|----------|
| Главная | `index.html` | Главная страница с hero секцией |
| Услуги | `services.html` | Список услуг прачечной |
| Советы | `tips.html` | Полезные советы по уходу за одеждой |
| Инструкции | `instructions.html` | Инструкции по использованию услуг |
| FAQ | `faq.html` | Часто задаваемые вопросы |
| Контакты | `contact.html` | Контактная информация |
| Локация | `location.html` | Местоположение прачечной |
| Детали совета | `tip-detail.html` | Детальная страница отдельного совета |
| 404 | `404.html` | Страница ошибки |

---

## 🔄 Как работают общие компоненты

### Header и Footer загружаются автоматически

Каждая страница содержит:

```html
<!-- Header Placeholder -->
<div id="header-placeholder"></div>

<!-- Ваш контент -->

<!-- Footer Placeholder -->
<div id="footer-placeholder"></div>

<script src="./assets/js/main.js"></script>
```

JavaScript (`main.js`) автоматически:
1. Загружает `includes/header.html` в `header-placeholder`
2. Загружает `includes/footer.html` в `footer-placeholder`
3. Подсвечивает активную страницу в навигации
4. Инициализирует мобильное меню

### Редактирование header/footer

Чтобы изменить навигацию или footer на всех страницах:
1. Отредактируйте `includes/header.html` или `includes/footer.html`
2. Обновите страницу в браузере
3. Изменения применятся ко всем страницам!

---

## 🎨 Перенос вёрстки из Next.js

### Шаг 1: Найдите компонент страницы в Next.js

Ваши страницы находятся в:
```
laundromat-preview/src/pagesLayer/
├── MainPage/
├── ServicesPage/
├── TipsPage/
├── InstructionsPage/
├── FaqPage/
├── ContactPage/
├── LocationPage/
└── TipsDetailPage/
```

### Шаг 2: Скопируйте JSX разметку

Откройте компонент страницы в Next.js и скопируйте JSX код.

### Шаг 3: Конвертируйте в HTML

**Было (Next.js):**
```jsx
<div className="container mx-auto px-4">
  <h1 className="text-4xl font-bold">{title}</h1>
  <p className="text-gray-600">{description}</p>
</div>
```

**Стало (HTML):**
```html
<div class="container mx-auto px-4">
  <h1 class="text-4xl font-bold">Заголовок</h1>
  <p class="text-gray-600">Описание</p>
</div>
```

**Изменения:**
- `className` → `class`
- `{переменные}` → замените на реальный текст
- Удалите импорты и экспорты
- Tailwind классы остаются такими же!

### Шаг 4: Вставьте в соответствующий HTML файл

Вставьте конвертированный код в секцию `<main>` нужной страницы.

---

## 🎯 Примеры конвертации

### Пример 1: Карточка услуги

**Next.js:**
```jsx
<div className="card">
  <div className="text-4xl mb-4">{icon}</div>
  <h3 className="text-xl font-heading font-bold mb-3">{title}</h3>
  <p className="text-gray-600">{description}</p>
</div>
```

**HTML:**
```html
<div class="card">
  <div class="text-4xl mb-4">👕</div>
  <h3 class="text-xl font-heading font-bold mb-3">Стирка одежды</h3>
  <p class="text-gray-600">Профессиональная стирка повседневной одежды</p>
</div>
```

### Пример 2: Ссылка на другую страницу

**Next.js:**
```jsx
<Link href="/services">Услуги</Link>
```

**HTML:**
```html
<a href="services.html">Услуги</a>
```

### Пример 3: Условный рендеринг

**Next.js:**
```jsx
{isActive && <div className="badge">Активно</div>}
```

**HTML:**
```html
<!-- Если нужно показать всегда -->
<div class="badge">Активно</div>

<!-- Если условно - используйте JavaScript или удалите -->
```

---

## 🔗 Навигация между страницами

Все ссылки в навигации уже настроены в `includes/header.html`:

```html
<a href="index.html">Главная</a>
<a href="services.html">Услуги</a>
<a href="tips.html">Советы</a>
<!-- и т.д. -->
```

Активная страница автоматически подсвечивается синим цветом.

---

## 📱 Адаптивность

Все Tailwind классы работают так же, как в Next.js:

```html
<!-- Адаптивная сетка -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <!-- Контент -->
</div>

<!-- Адаптивный текст -->
<h1 class="text-2xl md:text-4xl lg:text-6xl">Заголовок</h1>

<!-- Скрыть на мобильных -->
<div class="hidden md:block">Только на десктопе</div>
```

---

## 🔄 Конвертация в WordPress (позже)

Когда будете готовы конвертировать в WordPress:

### 1. Создайте файлы темы

```
wp-content/themes/laundromat/
├── style.css           # Метаданные темы
├── functions.php       # Регистрация стилей и меню
├── header.php          # Из includes/header.html
├── footer.php          # Из includes/footer.html
├── front-page.php      # Из index.html
├── page-services.php   # Из services.html
└── ...
```

### 2. Добавьте WordPress функции

**header.php:**
```php
<!DOCTYPE html>
<html <?php language_attributes(); ?>>
<head>
    <meta charset="<?php bloginfo('charset'); ?>">
    <?php wp_head(); ?>
</head>
<body <?php body_class(); ?>>
<!-- Ваш header из includes/header.html -->
```

**footer.php:**
```php
<!-- Ваш footer из includes/footer.html -->
<?php wp_footer(); ?>
</body>
</html>
```

### 3. Создайте functions.php

```php
<?php
function laundromat_scripts() {
    wp_enqueue_style('tailwind', get_template_directory_uri() . '/assets/css/output.css');
    wp_enqueue_script('main-js', get_template_directory_uri() . '/assets/js/main.js', array(), '1.0', true);
}
add_action('wp_enqueue_scripts', 'laundromat_scripts');
?>
```

---

## 💡 Полезные советы

### Работа с изображениями

Храните изображения в папке `public/images/`:

```html
<img src="public/images/logo.png" alt="Логотип" class="w-32 h-32">
```

### Добавление новой страницы

1. Скопируйте любой существующий HTML файл
2. Переименуйте его
3. Измените контент в `<main>`
4. Добавьте ссылку в `includes/header.html`

### Кастомный CSS

Добавляйте в `src/input.css`:

```css
@layer components {
  .my-custom-class {
    @apply bg-blue-500 text-white p-4 rounded-lg;
  }
}
```

Затем запустите `npm run build`.

---

## ✅ Готово к работе!

Всё настроено для переноса вашей вёрстки из Next.js:

1. ✅ 9 страниц созданы
2. ✅ Общий header и footer
3. ✅ Навигация работает
4. ✅ Tailwind CSS настроен
5. ✅ Адаптивный дизайн

**Начните с любой страницы:**
- Откройте соответствующий компонент в Next.js
- Скопируйте JSX разметку
- Конвертируйте в HTML (className → class)
- Вставьте в HTML файл
- Проверьте в браузере!
